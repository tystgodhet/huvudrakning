/* Client knobs. FAMILY_SYNC_URL empty = family row stays local-only
   (same as a device that never joined a family). After you deploy the
   worker, paste its URL here — no trailing slash — and bump sw.js VERSION.

   Example: "https://huvudrakning-family.your-subdomain.workers.dev" */

export const FAMILY_SYNC_URL = "https://huvudrakning-family.tystgodhet.workers.dev";
